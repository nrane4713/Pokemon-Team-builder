# Local dev: point at a local SQLite file, no auth token needed.
TURSO_DATABASE_URL="file:./dev.db"

# Production (Vercel): set these to your real Turso database instead.
# TURSO_DATABASE_URL="libsql://<your-db>.turso.io"
# TURSO_AUTH_TOKEN="..."
